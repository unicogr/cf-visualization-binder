#!/bin/bash
set -e

echo "🔧 Setting up Jupyter extensions..."

# Enable ipywidgets
jupyter nbextension enable --py widgetsnbextension --sys-prefix

# Enable voila
jupyter serverextension enable voila --sys-prefix

# Configure Voilà to show tracebacks
mkdir -p ~/.jupyter
cat > ~/.jupyter/jupyter_config.json << 'JSON'
{
  "VoilaConfiguration": {
    "show_tracebacks": true,
    "log_level": "DEBUG"
  }
}
JSON

echo "✅ Setup complete!"
